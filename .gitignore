# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Django
*.log
media/
staticfiles/
backend/staticfiles/

# Node
node_modules/
dist/
.vite/
.turbo/
coverage/
# ...but apps/strategy/coverage/ is the COVERAGE source package, not a report dir —
# negate it so `git add` still tracks it.
!backend/apps/strategy/coverage/
.pnpm-store/

# uv
.uv-cache/

# mutmut (mutation testing — nightly; writes a mutants/ working copy)
mutants/
.mutmut-cache

# Env / secrets
.env
.env.local
*.pem
*.key
/data/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Brainstorming companion workspace
.superpowers/

# Git worktrees
.worktrees/
.clone/
.claude/worktrees/

# MCP server scratch / session artifacts
.playwright-mcp/

*storybook.log
storybook-static

# CodeQL local security scans (see codeql-config.yml; DB built under /tmp)
codeql-dbs/
*.sarif

# Visual-diff failure artifacts — written next to the baseline on mismatch by
# e2e/helpers/visual.capture_or_compare; inspect locally, never commit.
e2e/visual/__screenshots__/*.actual.png

# Perf-lane advisory metrics — written per run by e2e/perf/test_perf_budgets.py;
# inspect locally, never commit.
e2e/perf/artifacts/